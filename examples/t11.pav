$D+ // Turn diagnostic mode on for testing the compiler - t11.pav

void main () { // Compound type casting
  enum MyType { a, b, c };
  MyType x, y;
  int i = 0;
  for x = a to c {
    write((char) ( (int) x + 65));
    write(cast(MyType, (char) ( (int) x + 65)), " "); // should this be allowed?
  }
}

